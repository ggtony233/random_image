services:
  random_image:
    image: 'random_image:local'
    container_name: random_image
    volumes:
      - '/home/ggtony/Downloads:/app/images'
    ports:
      - '8080:8080'
    restart: unless-stopped